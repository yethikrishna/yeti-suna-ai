# Copy this file to .env and fill in your values

# Environment Mode
# Valid values: local, staging, production
ENV_MODE=local

# DATABASE Configuration
# Choose 'sqlite' for local default, or 'supabase' for cloud/advanced.
DATABASE_TYPE=sqlite

# Local Data Directory (for SQLite DB, local file uploads, etc.)
# This path is relative to the backend directory if not absolute.
DATA_DIR=data_files

# SQLite Configuration (used if DATABASE_TYPE=sqlite)
# Path is relative to DATA_DIR if not absolute. Default: data_files/suna_local.db
SQLITE_DB_PATH=suna_local.db

# Supabase Configuration (ONLY needed if DATABASE_TYPE=supabase)
# SUPABASE_URL=your_supabase_url
# SUPABASE_ANON_KEY=your_supabase_anon_key
# SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# --- Local Services (Defaults for Docker Compose) ---
# These are typically managed by Docker Compose for local development.
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_SSL=false

RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672

# --- Required External Services: LLM Providers ---
# At least one LLM API key is usually needed for the application to be useful.
# MODEL_TO_USE defines which model configuration will be used by default.
# Example: "openai/gpt-4o", "anthropic/claude-3-opus-20240229", "openrouter/google/gemini-pro"
MODEL_TO_USE=openai/gpt-4o-mini

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
MODEL_TO_USE=

# AWS Bedrock (if used as LLM provider, also requires AWS credentials below)
# GROQ_API_KEY=
# OPENROUTER_API_KEY=
# DEEPSEEK_API_KEY=

# --- Optional External Cloud Services ---
# These services enhance functionality but are not strictly required for a basic local setup.

# Observability & Monitoring:
# Sentry (Error Tracking - Optional)
# SENTRY_DSN=

# Langfuse (LLM Tracing & Analytics - Optional)
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_SECRET_KEY=
# LANGFUSE_HOST=https://cloud.langfuse.com # Default if using Langfuse Cloud

# AWS Credentials (Used for Bedrock LLM provider and potentially other AWS services - Optional)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION_NAME=

# Web Search Tools (Optional - for agents needing web search)
# TAVILY_API_KEY=

# Web Scraping Tools (Optional - for agents needing web scraping)
# FIRECRAWL_API_KEY=
# FIRECRAWL_URL=https://api.firecrawl.dev # Default

# Other Data APIs (Optional)
# RAPID_API_KEY=

# Sandbox Container Provider (Optional - Daytona for dynamic sandboxes)
# DAYTONA_API_KEY=
# DAYTONA_SERVER_URL=
# DAYTONA_TARGET=

# Smithery (Unknown purpose, likely optional)
# SMITHERY_API_KEY=

# Stripe (Billing - Not typically needed for local development focus)
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=